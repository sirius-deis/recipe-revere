import { FC } from "react";

interface Ingredient {
    quantity: number,
    measure: string,
    food: string,
    weight: number,
    foodId: string,
    image: string
}

const IngredientsList: FC = ({}) => {
    return <ul>

    </ul>
}

export default IngredientsList;